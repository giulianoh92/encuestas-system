CREATE TABLE csv_errores_log (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    linea_csv TEXT NOT NULL,
    error_descripcion TEXT NOT NULL,
    fecha_error TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);